kind: Service
apiVersion: v1
metadata:
  name: pp-ambulance-webapi
spec:
  selector:
    pod: pp-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port